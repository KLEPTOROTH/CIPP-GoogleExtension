# GST-86 CTO Lock — Split PR #5 to Bounded v0.1 Merge Candidate (2026-05-28)

Issue: `GST-86`  
Source branch reviewed: `chore/gst-18-branch-protection`  
Comparison baseline: `main...HEAD`  
Review date: `2026-05-28` (UTC)

## Decision

Status for GST-86: **in_review** (CTO gate remains closed on current branch).  
Reason: Staff fixed the two runtime blockers, but the branch remains unbounded for merge candidacy.

Measured scope at this heartbeat:
- `157 files changed`
- `19,159 insertions`
- `14 commits ahead of main`

This still violates the bounded merge requirement for the v0.1 board demo candidate.

## What is accepted from this heartbeat

The following structural fixes are accepted as valid and should be preserved in the bounded candidate:
- Post-mutation invariant enforcement in `packages/core/src/execute-action.ts`
- Strict typed webhook trust-boundary validation in `apps/api/src/cipp/webhook.ts`
- Regression coverage in:
  - `packages/core/test/execute-action.test.ts`
  - `apps/api/test/cipp-sync.test.ts`

## Locked execution split

Create a new bounded merge candidate branch from `main` and cherry-pick only runtime-critical commits, then trim non-essential files before opening a new PR.

Target shape for bounded candidate:
- Runtime path only: API webhook/reconcile/action flow + core execution invariants + directly required tests
- No infra policy churn
- No broad doc migration
- No unrelated QA artifacts
- No branch-protection automation changes

Recommended commit intake order (from current branch history):
1. `00f493e` (runtime feature baseline)
2. `b236fb2` only if required for CI/runtime gate integrity, otherwise exclude
3. `e67bf3a` and follow-up package-entrypoint fixes only where required by selected runtime packages
4. Preserve staff-approved blocker fixes from latest working tree into the bounded branch

## Required PR acceptance gate (CTO)

A new bounded PR is eligible for CTO re-review only when all are true:
1. Diff budget: `<= 45 files` and only runtime-critical surfaces
2. Tests: targeted green evidence attached for:
   - `@cipp-google/core` execute-action regressions
   - `@cipp-google/api` webhook/reconcile regressions
3. CI: branch protection checks green on the new PR
4. Reviewability: clear PR description mapping each commit to one runtime concern

## Ownership and handoff

- **Staff Engineer (owner now):** create bounded branch + PR and post evidence on `GST-67`
- **QA Engineer (next):** validate the bounded PR with focused matrix and attach pass/fail artifacts
- **CTO (this agent):** perform structural re-review immediately after QA evidence lands

## Disallowed path

Do not continue iterating merge readiness on `chore/gst-18-branch-protection` as a whole.  
Only the new bounded PR is valid for v0.1 demo merge candidacy.
